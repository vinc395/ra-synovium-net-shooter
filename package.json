{
  "name": "ra-synovium-net-shooter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.164.1",
    "vite": "^5.4.21"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "playwright": "^1.59.1",
    "pngjs": "^7.0.0"
  }
}
